services:
  - type: web
    name: inspec360-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install --no-optional --legacy-peer-deps
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
